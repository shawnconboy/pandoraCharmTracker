import SwiftUI

struct FriendsListView: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("Friends List Coming Soon!")
                .font(.headline)
                .padding(.top)
        }
        .frame(maxWidth: .infinity)
    }
}
